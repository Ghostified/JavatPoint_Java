package Arrays;
/*
 * Declare, intantiate and initialize in one line an array
 */
public class Array2 {
    public static void main (String[]Args) {

        //instatiation, declation and initialization in one line
        int a [] = {4,5,6,7}; 

        //print 
        for (int i= 0 ; i < a.length ; i++)
        System.out.println(a[i]);
    }
}
